#!/bin/sh

# SPDX-FileCopyrightText: 2025 hexaTune LLC
# SPDX-License-Identifier: MIT

# Commit message hook for Commitlint validation
# This hook ensures commit messages follow conventional commit format

CONFIG=".github/.commitlintrc.yml"
MSG_FILE="$1"

# Add hexawebshare/node_modules/.bin to PATH for commitlint
export PATH="hexawebshare/node_modules/.bin:$PATH"

echo "🔍 Running Commitlint check..."

# Check if commitlint is installed
if ! command -v commitlint >/dev/null 2>&1; then
    echo ""
    echo "❌ ERROR: Commitlint CLI not found!"
    echo ""
    echo "📦 Commitlint is required for commit message validation."
    echo ""
    echo "🔧 Installation Instructions:"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    echo "Navigate to hexawebshare directory and install:"
    echo "  $ cd hexawebshare"
    echo "  $ pnpm add -D @commitlint/cli @commitlint/config-conventional"
    echo ""
    echo "Or with npm:"
    echo "  $ npm install -D @commitlint/cli @commitlint/config-conventional"
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    exit 1
fi

# Check if config file exists
if [ ! -f "$CONFIG" ]; then
    echo ""
    echo "❌ ERROR: Commitlint config not found!"
    echo ""
    echo "Expected location: $CONFIG"
    echo "Please ensure the config file exists before committing."
    echo ""
    exit 1
fi

# Run commitlint
echo "📌 Validating commit message format..."
echo ""

if ! commitlint --config "$CONFIG" --edit "$MSG_FILE"; then
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "❌ Commit message validation FAILED!"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    echo "📚 Commit message must follow Conventional Commits format:"
    echo ""
    echo "   <type>(<optional-scope>): <description>"
    echo ""
    echo "Allowed types:"
    echo "  • feat     - New feature"
    echo "  • fix      - Bug fix"
    echo "  • docs     - Documentation changes"
    echo "  • style    - Code formatting (no logic change)"
    echo "  • refactor - Code restructuring"
    echo "  • perf     - Performance improvement"
    echo "  • test     - Test changes"
    echo "  • chore    - Maintenance tasks"
    echo "  • ci       - CI/CD changes"
    echo "  • build    - Build system changes"
    echo "  • release  - Release preparation"
    echo "  • hotfix   - Critical emergency fix"
    echo ""
    echo "Examples:"
    echo "  ✅ feat(Button): add loading state prop"
    echo "  ✅ fix(Modal): resolve focus trap issue"
    echo "  ✅ docs(README): update installation steps"
    echo "  ✅ refactor: simplify button component logic"
    echo ""
    echo "See docs/COMMIT_STRATEGY.md for more details."
    echo ""
    exit 1
fi

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ Commit message validation PASSED!"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
exit 0
